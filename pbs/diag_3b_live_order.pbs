#!/bin/bash
#PBS -P li96
#PBS -q gpuvolta
#PBS -l ngpus=1,ncpus=12,mem=96GB
#PBS -l walltime=02:00:00
#PBS -l storage=gdata/li96+scratch/li96
#PBS -l wd
#PBS -N diag_3b_live
#PBS -j oe
#PBS -o /scratch/li96/mz9869/logs/

# 3b live order confrontation on 4 σ-drift prompts (gate#2). No threshold loosen.

set -euo pipefail
export HF_HOME=/scratch/li96/mz9869/tmp_hf_download
export HF_HUB_OFFLINE=1
export TRANSFORMERS_OFFLINE=1

PROJECT_ROOT="${HOME}/medusa-qwen"
VENV="${HOME}/medusa-env"
C1=/scratch/li96/mz9869/medusa_outputs/linked_medusa_c1_eagle
OUT=$C1/diag_triad/item6_live_order

echo "=== 3b LIVE ORDER $(hostname) $(date) job=${PBS_JOBID:-N/A} ==="
module load python3/3.11.0 cuda/12.3.2 2>/dev/null || true
source "${VENV}/bin/activate"
cd "${PROJECT_ROOT}"
mkdir -p "$OUT" /scratch/li96/mz9869/logs

python3 -u -m scripts.diag_item6_live_order \
  --ckpt "$C1/ckpt_best.pt" \
  --indices 10 58 78 85 \
  --fanout 1 6 4 2 1 \
  --max-nodes 24 \
  --out-dir "$OUT"

echo "=== DONE $(date) ==="
